<!-- ======= Footer ======= -->
<footer class="relative bottom-0 w-full bg-gray-900 shadow-inner py-4 mt-8 glass">
    <div class="container mx-auto px-4 text-center text-gray-400">
        <p>&copy; {{ "now"|date('Y') }} IWF - Internet Wrestling Federation. All rights reserved.</p>
        <p><small>IWF / Internet Wrestling Federation is in no way associated with the WWE or any other professional wrestling promotion.</small></p>
    </div>
</footer>
<script>
// Get the full path from the browser's URL
const fullPath = window.location.pathname;

// The 'baseUrl' variable is globally available from your main.twig file
// It will contain the value from your .env, like "/iwf-app"
const appPath = fullPath.replace(baseUrl, '');

// If the path is empty (meaning you're at the root), default to '/'
const currentPage = appPath || '/';

// console.log(currentPage); // If the URL is http://localhost/iwf-app/simulator, this will log "/simulator"
</script>
<!-- Global App Scripts -->
<script type="module" src="{{ base_url }}/public/js/app.js"></script>
<script src="{{ base_url }}/public/js/notifications.js"></script>

{% if 'booking' in current_path %}
    <script type="module" src="{{ base_url }}/public/js/booking.js"></script>
{% endif %}

{% if 'tournament' in current_path %}
    <script type="module" src="{{ base_url }}/public/js/tournament.js"></script>
{% endif %}

{% if 'simulator' in current_path %}
    {# <script type="module" src="{{ base_url }}/public/js/match-simulator.js"></script> #}
{% endif %}